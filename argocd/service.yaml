apiVersion: v1
kind: Service
metadata:
  name: sk032-backend-argocd
  namespace: argocd-demo
spec:
  selector:
    app: sk032-backend-argocd
  ports:
    - name: http
      port: 5000
      targetPort: 5000
  type: ClusterIP
